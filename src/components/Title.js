import React from 'react';

const Title = () => (
    <div>
        <h1 className="title-container_title">Weather Finder</h1>
        <p className="title-container_subtitle">Find out temperature, conditions and more...</p>
    </div>
);

export default Title;